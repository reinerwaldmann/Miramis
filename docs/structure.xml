<!-- структура файла результатов  -->


<root>
    <typeOfReport>NIST</typeOfReport>
    <model> модель изделия </model>
    <typeOfTest>тип испытания  </typeOfTest>
    <channelnames>
    <!-- имена каналов -->
        <channel> имя канала изделия, например Канал 1 5 В. Так и только так должны называться каналы в описании результатов в дальнейшем</channel>

    </channelnames>

    <proceduresResults >
        <result number="номер порядковый">
            <name>название процедуры </name>
            <modes_common>
                <!--Описание общих режимов, при которых производится испытание, выраженное в парах имя параметра - значение параметра.
                Значение параметра парсится как строка, потому может в себя включать разные символы, например, "5В"
                Вместо секции mode может быть представление в виде словаря Python, то есть {'имя режима':'значение режима'} ,
                и оное представление даже предпочтительнее.
                -->
                <mode_com>
                    <modename>имя режима</modename>
                    <modevalue>значение режима</modevalue>
                </mode_com>
            </modes_common>

            <modes_channel>
                <!--Описание режимов по каналам, при которых производится испытание, выраженное в парах имя параметра - значение параметра, применительно к каждому каналу.
                Значение параметра парсится как строка, потому может в себя включать разные символы, например, "5В"
                Вместо секции mode может быть представление в виде словаря словарей Python, то есть {'имя канала':{ 'имя режима':'значение режима'} } ,
                и оное представление даже предпочтительнее.
                -->

                <channel_modes channame="имя канала, точно как в списке имён каналов">
                    <mode_chan>
                        <modename>имя режима</modename>
                        <modevalue>значение режима</modevalue>
                    </mode_chan>
                </channel_modes>

            </modes_channel>



        </result>


    </proceduresResults>












</root>